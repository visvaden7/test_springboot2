spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.MySQL5InnoDBDialect"
  h2:
    console:
      enabled: true
  mustache:
    charset: "UTF-8"
    suffix: ".mustache"
    prefix: "classpath:/templates/"
    cache: false